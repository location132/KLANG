import 'package:flutter/material.dart';

class MarketingAgreedPage extends StatelessWidget {
  const MarketingAgreedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('마케팅 정보 수신 동의 화면입니다.'),
      ),
    );
  }
}
