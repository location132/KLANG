import 'package:flutter/material.dart';

class ServiceAgreedPage extends StatelessWidget {
  const ServiceAgreedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('서비스 이용약관 동의 필수 화면입니다'),
      ),
    );
  }
}
