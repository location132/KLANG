import 'package:flutter/material.dart';

class PrivacyAgreedPage extends StatelessWidget {
  const PrivacyAgreedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('개인정보 취급방침 동의 화면입니다.'),
      ),
    );
  }
}
